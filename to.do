(library(sgml).)

(figure out how to make changes to files stored in git-annex)

(looking at deductive databases des.sourceforge.net and xsb's
 odbc connector with a mysql driver)

(libmyodbc)

(when I get my NAS system, have our KBFS store metadata about the
 system, maybe using git-annex and XSB w/ ODBC, or whatever
 deductive database or Jess' one, assert information about the
 files themselves.)

(http://xsb.sourceforge.net/shadow_site/manual2/node76.html)
(http://www.swi-prolog.org/pldoc/package/odbc.html)
(http://sourceforge.net/projects/des/?source=typ_redirect)
(http://xsb.sourceforge.net/about.html)

(git annex metadata --tag='<tagname>' <file>
 git annex metadata --untag='<tagname>' <file>
 git annex metadata <file>)

(http://git-annex.branchable.com/tips/metadata_driven_views/)

(check out ciao in external, set it up and try it out)
